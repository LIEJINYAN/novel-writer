# 收尾 Open Questions — 验证检查清单

## 编辑器行号
- [x] 编辑器左侧显示行号，与文本同步滚动
- [x] 增删行后行号自动更新
- [x] 两个主题下行号颜色清晰可读

## 撤销/重做
- [x] 工具栏有撤销/重做按钮
- [x] Ctrl+Z 撤销，Ctrl+Y 重做
- [x] 无操作时按钮置灰

## 自动保存配置
- [x] 设置对话框可调节自动保存间隔（AI 设置对话框内）
- [x] 修改后新间隔即时生效
- [x] 默认 30 秒

## 续写字数选择
- [x] AI 面板有字数选择器，默认 2000
- [x] 续写请求携带字数参数
- [x] 生成内容不超过指定字数

## API Key 加密（确认项）
- [x] `utils/crypto.py` 使用 cryptography 库
- [x] `encrypt_api_key` / `decrypt_api_key` 正常工作

## Ollama 适配器（确认项）
- [x] `OllamaProvider` 使用 httpx 调用 REST API
- [x] `chat_stream()` 正确解析 ndjson

## 拖拽排序确认 + 撤销
- [x] 拖拽后有确认对话框
- [x] 取消则还原排序
- [x] 提供撤销入口恢复拖拽前状态

## 大纲面板跳转
- [x] 单击大纲节点跳转到对应章节编辑器
- [x] 已打开的标签页切换到该标签页
- [x] 未打开的标签页新建标签页
